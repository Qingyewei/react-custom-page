import React from 'react'

/**
 * 写一个Search组件，利用getSearchComponent
 * getTable 写入Table组件
 * 渲染时直接不封装
 * 要满足一下几个问题：
 * 1、可以动态添加SearchItem
 * 2、Table的column可以动态添加
 * 3、可以动态添加按钮或者标题等信息之类
*/
function RenderTable() {
  return (
    <div>
      
    </div>
  )
}

export default RenderTable
